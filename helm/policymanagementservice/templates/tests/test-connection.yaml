apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "policymanagementservice.fullname" . }}-test-connection"
  labels:
    {{- include "policymanagementservice.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "policymanagementservice.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
